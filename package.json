{
  "name": "fifteen",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --port 8080"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.7"
  }
}
